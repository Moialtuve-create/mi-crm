import { PantallaPendiente } from "@/components/ui/PantallaPendiente";

export default function ClientesPage() {
  return (
    <PantallaPendiente
      titulo="Clientes"
      tarea="MOI-35 (lista) y MOI-34 (nuevo/editar overlay)"
      referencia='design/.../README.md → "3. Lista de clientes" y "5. Nuevo / Editar cliente"'
    />
  );
}
